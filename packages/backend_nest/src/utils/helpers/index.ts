export const messagesHelper = {
  PASSWORD_VALID: 'The password must contain lowercase letters, numbers and special characters',
  PASSWORD_OR_EMAIL_INVALID: 'E-mail and / or password are invalid',
};

export const regexPassdHelper = /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$ %^&*-]).{8,}$/;
